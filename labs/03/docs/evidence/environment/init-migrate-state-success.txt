# Step 7 — state migrated from the local backend to S3, after the group's state
# bucket (eda-tfstate-grupo07) was provisioned by infra/tfstate-backend.
# Supersedes init-migrate-state-failed-backend-missing.txt, kept as the record
# of why the bootstrap stack had to exist.
# captured: 2026-09-15T23:31Z (local terminal) · Terraform v1.16.1

$ terraform init -migrate-state -backend-config=backend.hcl
Initializing the backend...
Do you want to copy existing state to the new backend?
  Pre-existing state was found while migrating the previous "local" backend to the
  newly configured "s3" backend. No existing state was found in the newly
  configured "s3" backend. Do you want to copy this state to the new "s3"
  backend? Enter "yes" to copy and "no" to start with an empty state.

  Enter a value: yes


Successfully configured the backend "s3"! Terraform will automatically
use this backend unless the backend configuration changes.

Initializing modules...

Initializing provider plugins...
- Reusing previous version of hashicorp/aws from the dependency lock file
- Using previously-installed hashicorp/aws v5.100.0

╷
│ Warning: Deprecated Parameter
│
│   on backend.tf line 8, in terraform:
│    8:   backend "s3" {
│
│ The parameter "dynamodb_table" is deprecated. Use parameter "use_lockfile" instead.
╵
Terraform has been successfully initialized!

You may now begin working with Terraform. Try running "terraform plan" to see
any changes that are required for your infrastructure. All Terraform commands
should now work.

If you ever set or change modules or backend configuration for Terraform,
rerun this command to reinitialize your working directory. If you forget, other
commands will detect it and remind you to do so if necessary.

# Notes
# - "No existing state was found in the newly configured s3 backend" confirms the
#   key aula12/brlla/terraform.tfstate was free: nothing of a teammate's was
#   overwritten by this migration.
# - The dynamodb_table deprecation is expected: Terraform >= 1.11 prefers the
#   S3-native use_lockfile. The course requires the DynamoDB table, so the
#   deprecated parameter is kept deliberately (see DECISOES.md).
